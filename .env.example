# Copy to .env and edit. Used by app/config/config.php.
# Never commit .env — it is gitignored.

# XAMPP / WAMP / local MySQL typically:
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=the_farmer
DB_USER=root
DB_PASS=

# Set to 1 only on a local machine while debugging.
APP_DEBUG=0
